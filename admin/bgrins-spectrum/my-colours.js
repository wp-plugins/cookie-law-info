jQuery(function() {
	jQuery("#cookie-cp1, #cookie-cp2, #cookie-cp3, #cookie-cp4, #cookie-cp5, #cookie-cp6").spectrum({
		showPalette: false,
		palette: [
			['#f00', '#ff0', '#0f0'], 
			['#00f', '#333', '#fff']
		],
		showInitial: true,
		preferredFormat: "hex"
	});
});